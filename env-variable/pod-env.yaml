apiVersion: v1 
kind: Pod
metadata:
  name: env-pod
spec:
  containers:
  - name: demo
    image: busybox
    command: ["sleep", "3600"]
    env:
    - name: DB_HOST
      value: "prod-db.kplabs.internal"
    - name: DB_PASS 
      value: "notesecretpassword"
