apiVersion: v1
kind: Service
metadata:
  namespace: test
  labels:
    app: nginx
  name: nodeport-service
spec:
  ports:
    - port: 80
      nodePort: 32000
      targetPort: 80
  selector:
    app: nginx
  type: NodePort
status:
  loadBalancer: {}
