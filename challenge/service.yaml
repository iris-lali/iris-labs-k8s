apiVersion: v1
kind: Service
metadata:
  labels:
    app: app-service
  name: app-service
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    common: app
  type: ClusterIP

