# Kubernetes documentation
https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.32/
#ReplicaSet
https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
#DaemonSet
https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/
https://artifacthub.io/
https://helm.sh/docs/intro/install/

# Topology Spread
https://kubernetes.io/docs/concepts/scheduling-eviction/topology-spread-constraints/

### External Website Referenced

https://artifacthub.io/

https://artifacthub.io/packages/helm/bitnami/nginx

### Install Nginx Chart

```sh
helm repo add bitnami https://charts.bitnami.com/bitnami

helm install my-nginx bitnami/nginx --version 19.0.4
```

### Other Helm Commands
```sh
helm list

helm repo list

helm search repo bitnami

helm search hub nginx

helm template bitnami/nginx

helm template bitnami/nginx > nginx.yaml

helm template bitnami/nginx -n custom-ns > nginx.yaml

helm uninstall my-nginx
```

###############################

When dealing with multiple clusters

kubectl config get-contexts  # to get the config for all the clusters
kubectl config use-context << context-name>>

### Namespaces
kubectl get pods --all-namespaces
kubectl get configmaps
kubectl get namespace
kubectl create namespace qa # To create a namespace
kubectl run qa-pod --image=nginx -n qa
kubectl config set-context --current --namespace=default

## Seviceaccounts
kubectl create serviceaccount custom-sa
kubectl create sa custom-sa
kubectl get serviceaccount
kubectl get sa
kubectl get sa -n development


kubectl run nginx --image=nginx --dry-run=client -o yaml > custom-pod.yaml
kubectl apply -f pod.yaml
kubectl delete -f pod.yaml
kubectl exec -it nginx -- bash
kubectl exec -it multi-container-pod -c redis-container -- bash
kubectl run echo-pod --image=busybox:latest --command -- echo "Hello World!"
kubectl api-resources

##########################################################################
# Explain command, one of the best way to learn how to write a manifest

kubectl explain pod
kubectl explain pod.spec.containers

############################################################################

Kubernetes Tools

winget install Kubernetes.kind  ## To install kind on windows

https://kind.sigs.k8s.io/docs/user/quick-start/

kind create cluster --name=iris-cluster # To create cluster in docker
kind create cluster --name iris-multi --config=multi-nodes-cluster.yaml # Create a multi nodes cluster
king get clusters
kind delete cluster --name=<<cluster-name>>
kubectl config view # to view all the clusters
kubectl config view | grep kind # to filter all the cluster created with kind.
kubectl config use-context kind-iris-multi # toswich to iris-multi cluster

############################################################

kubectl get pods --show-labels
kubectl label pod pod1 env=dev
kubectl get pods -l env!=dev
kubectl get pods -l env=dev
kubectl scale --replicas=1 rs/frontend-replicaset
kubectl get replicaset
kubectl get rs
kubectl delete rs frontend-replicaset
kubectl describe rs/webserver-replicaset
kubectl delete -f rs.yaml
kubectl rollout history deployment/nginx-deployment
kubectl create deployment nginx-deployment --image=nginx
kubectl set image --help
kubectl set image deployment/nginx busybox=busybox nginx=nginx:1.9.1
kubectl rollout undo deployment nginx-deployment
kubectl set image deployment/nginx-deployment nginx=httpd:latest
kubectl rollout undo deployment nginx-deployment --to-revision=2
kubectl rollout history deployment deploy --revision=2
kubectl scale --replicas=3 deployment nginx-deployment
kubectl delete deployment nginx-deployment
kubectl create deployment demo-deployment --image=nginx --replicas 3
kubectl create deployment demo-deployment --image=nginx --replicas=3
kubectl get deployment demo-deployment -o yaml
kubectl edit deployment demo-deployment
kubectl get pods -o wide
apt-get update && apt-get install curl -y
kubectl create -f endpoints.yaml
kubectl create ingress firt-ingress --rule="example.internal/*=example-service:80" # Create ingress controler rules
kubectl create ingress second-ingress --rule="example.internal/*=example-service:80" --rule="kplabs.internal/*=kplabs-service:80"
kubectl get resourcequota -n quota-demo
kubectl get quota -n quota-demo

kubectl top pods -A
kubectl top nodes
kubectl get pods --all-namespaces --field-selector metadata.namespace!=default

kubectl get pv
kubectl get pvc
kubectl delete pv 
kubectl delete pvc
kubectl get cm first-configmap -o yaml # to get the description of the configmap 
kubectl describe cm first-configmap  # to get the description of the configmap 
kubectl create configmap first-configmap --from-literal=key1=value1 --from-literal=key2=value2 # To create a configmap using literal 
kubectl create configmap --help
kubectl create cm second-configmap --from-file=large-file.txt
kubectl create configmap second-configmap --from-file=large-file.txt

### Kubectl quick reference
https://kubernetes.io/docs/reference/kubectl/quick-reference/

######################################################################
Docker Commands

docker pull nginx:1.14.2
docker tag nginx:1.14.2 irislali12/nginx:1.14.2:v1
docker push irislali12/nginx:1.14.2:v1

docker build -t irislali12/crashlooptest:v1 .
docker rmi irislali12/crashlooptest:v1

kubectl create secret docker-registry  \
  --docker-server=${AWS_ACCOUNT}.dkr.ecr.${AWS_REGION}.amazonaws.com \
  --docker-username=AWS \
  --docker-password=$(aws ecr get-login-password) \
  --namespace=default

kubectl create secret docker-registry demo --docker-server=https://index.docker.io/v1/ --docker-username= --docker-password= --docker-email=  


############### Docker imagepullsecret, link for docker registry
https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/  


###############################
Taint a node 
kubectl taint nodes <<node name>> key=value1:NoSchedule

kubectl scale --help
kubectl run --help
kubectl explain deployment --recursive

##############################
############ Cronjob ################
# In crojob Timeout reffers to activeDeadlineSeconds which is defined under spec 
in jobtemplate and Delay refers to startingDeadlineSeconds under spec

##############################################

## kube config EKS ##########
# C:\Users\lalif\.kube\config
################# YHlTVI6pUHzWKGsf
elastic
grafana
OiPhzsaQBtjpV1jlKegz5nmEnLNWE31aIMtK1XmC
decode :#ƐWX)3yV}Z Jy