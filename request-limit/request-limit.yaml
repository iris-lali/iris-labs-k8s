apiVersion: v1
kind: Pod
metadata:
   name: kplabs-container
spec:
  containers:
    - name: kplabs-container
      image: nginx
      resources:
        requests:
          memory: "128Mi"
          cpu: "0.1"
        limits:
          memory: "500Mi"
          cpu: "1"